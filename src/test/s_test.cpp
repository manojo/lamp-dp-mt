#include "include/common.h"

/*
- nonserial: use more than 1 thread to compute best
- discuss with Nithin whether best to use diagonal or verticals
- memoize some main diagonal/horizontals in main memory
- use any device to launch the computation
  + fpga
  + gpu
  + thread

*/


int main(int argc, char** argv) {
	printf("Serial\n");
	return 0;
}